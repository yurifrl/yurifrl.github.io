version: '2'
services:
  web:
    image: yurifl/octopress
    command: jekyll serve --watch
    ports:
      - 4000:4000
    volumes:
      - .:/srv/jekyll
      # - deploy:/srv/jekyll/deploy
      # - sass-cache:/srv/jekyll/.sass-cache

# volumes:
#   deploy:
#     external: true
#   sass-cache:
#     external: true

